class IdaoArticle {
    // Fonction vide en JS vu qu'on les écrase dans doaArticleSequelize
    insert(article) {};
    selectAll() {};
    modified(article) {};
    delete(article) {};
}

module.exports = IdaoArticle;